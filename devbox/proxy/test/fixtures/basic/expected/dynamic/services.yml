# Generated by devbox/proxy/bin/generate-traefik-config.sh from services.yaml.
# Do not edit: change the declaration file and run `devbox-proxy reload`.

tls:
  stores:
    default:
      defaultCertificate:
        certFile: /state/certs/origin.pem
        keyFile: /state/certs/origin.key
  certificates:
    - certFile: /state/certs/origin.pem
      keyFile: /state/certs/origin.key

http:
  middlewares:
    cf-access-llama:
      forwardAuth:
        address: "http://127.0.0.1:9101/verify?service=llama&aud=7d1f0c1e5a3b4c6d8e9f0a1b2c3d4e5f60718293a4b5c6d7e8f9a0b1c2d3e4f5"
        authResponseHeaders:
          - X-Devbox-Access-Email
          - X-Devbox-Access-Sub
  routers:
    llama:
      rule: "Host(`llama.example.org`)"
      entryPoints:
        - websecure
      service: llama
      middlewares:
        - cf-access-llama
      tls: {}
  services:
    llama:
      loadBalancer:
        servers:
          - url: "http://127.0.0.1:8081"
